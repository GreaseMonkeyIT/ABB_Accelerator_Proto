# dashboard (P6)
Go WS backend + Next.js static export. Endpoints: /api/graph /api/timeline /api/insights WS /live POST /api/scenario/{id}.
Scenario console buttons map to ../scenarios/S*/trigger.sh equivalents via the K8s API.
